{
  "name": "mern-task-tracker-root",
  "version": "1.0.0",
  "description": "Root configuration to manage client and server workspace for MERN Task Tracker",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend"
  }
}
